/**
 * Theme variants for the Wingspan Score App
 * Currently only light theme is implemented
 */

import { theme } from './theme';

// Light theme (current default theme)
export const lightTheme = theme;

// Dark theme (placeholder - to be implemented)
// For now, using the same theme for both light and dark modes
export const darkTheme = theme;
